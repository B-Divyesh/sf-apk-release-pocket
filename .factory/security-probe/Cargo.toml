[package]
name = "arp-security-probe"
version = "0.0.0"
edition = "2024"

[dependencies]
apksig = { version = "0.4.0", default-features = false, features = ["signing"] }
rsa = { version = "0.9", features = ["sha2"] }

